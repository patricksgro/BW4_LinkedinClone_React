# 🚀 LinkedIn Clone - React & Bootstrap

Questo progetto è un **clone di LinkedIn** sviluppato in **React** con **Bootstrap (ultima versione)**, pensato per mettere in pratica lo sviluppo front-end moderno, l’integrazione con API e la gestione di interfacce complesse.

## 📌 Funzionalità principali

- **Routing completo** per navigare tra le varie pagine dell'applicazione.
- **Integrazione con API REST** con gestione di:
  - `GET` → recupero di dati (profili, esperienze, ecc.)
  - `POST` → aggiunta di nuove informazioni (esperienze, foto profilo, ecc.)
  - `PUT` e `PATCH` → modifica dei dati esistenti.
- **Gestione profilo personale**:
  - Caricamento e modifica della **foto profilo**.
  - Modifica delle informazioni personali (nome, cognome, titolo, area, bio, ecc.).
  - Aggiunta e modifica delle esperienze lavorative.
- **Interazione con altri utenti**:
  - Visita dei profili altrui.
  - Visualizzazione delle informazioni pubbliche degli altri utenti.
- **Form dinamici** per inserimento e aggiornamento dati.
- **UI responsive** grazie a Bootstrap 5.

## 🛠️ Tecnologie utilizzate

- **React** (componenti funzionali, hooks, context API)
- **React Router** (navigazione client-side)
- **Bootstrap 5** (UI responsiva e componenti predefiniti)
- **Fetch API** per la comunicazione con il backend
- **JavaScript ES6+**
- **CSS personalizzato** per rifinire l’interfaccia

## 🎯 Obiettivi del progetto

- Riprodurre in maniera fedele la logica e l’interfaccia di un social network professionale.
- Esercitarsi nell’utilizzo di **API REST** e nella gestione delle operazioni CRUD lato front-end.
- Migliorare le competenze di **React**, **routing**, **gestione stato** e **integrazione grafica**.

## 📷 Anteprima

![Screenshot homepage](public/Screenshot%202025-08-11%20174704.png)

## ▶️ Avvio del progetto in locale

1. Clona la repository:
   ```bash
   git clone https://github.com/tuo-username/nome-repo.git

   ```

2. Entra nella cartella del progetto

```bash
cd BW4_LINKEDINCLONE_REACT

```

3. Installa le dipendenze

```bash
npm install

```

4. Avvia il server di sviluppo

```bash
npm start
```

4. Apri il browser su

```bash
http://localhost:3000